using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BookConversation2 : MonoBehaviour
{
    // Start is called before the first frame update
    // A delegate type that represents an event function
    public delegate void EventFunction();

    // An array of delegates that hold the event functions
    public EventFunction[] events;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
